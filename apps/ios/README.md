# Alchemy iOS

This folder is a Swift package for the first iOS-side contract and onboarding shell.

It is not a full Xcode app target yet. The package exists so the setup-code parser, onboarding state, and SwiftUI flow are testable immediately while the repo is still being scaffolded.

